export async function generateDailyQuote(userId: string) {
  console.warn("generateDailyQuote called (stub)");
  return { quote: "Be yourself; everyone else is already taken.", author: "Oscar Wilde" };
}
