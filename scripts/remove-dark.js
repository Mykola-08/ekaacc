const fs = require("fs"); const cp = require("child_process"); try { const files = cp.execSync("git grep -l \"dark:\" src").toString().trim().split("\n").filter(Boolean); files.forEach(f => { let content = fs.readFileSync(f, "utf8"); content = content.replace(/dark:[A-Za-z0-9_/-]+/g, ""); fs.writeFileSync(f, content); console.log("Updated", f); }); } catch(e) { console.error(e.message); }
